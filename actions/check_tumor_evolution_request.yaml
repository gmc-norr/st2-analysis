---
name: check_tumor_evolution_request
runner_type: python-script
description: Check if there is a request for generating a tumor evolution report
enabled: true
entry_point: check_tumor_evolution_request.py

parameters:
  watch_file:
    type: string
    description: The file to watch for new requests
    required: true
    position: 0
    default: "{{ config_context.tumor_evolution.watch_file }}"

output_schema:
  type: object
  properties:
    message:
      type: string
    error:
      type: string
    excel_file:
      type: string
      format: uri-reference
    sheet:
      type: string
